'use strict';

const options = require('./web/options');
const profiles = require('./web/profiles');
const webConfigs = require('./web/webConfigs');
const errorCodes = require('./messages/error-codes');
const successCodes = require('./messages/success-codes');

module.exports = {
  options,
  profiles,
  webConfigs,
  errorCodes,
  successCodes,
};
