'use strict';

const errorCodes = {
  NotFoundConfigKeyManagerJSON: {
    message: 'Not found configure keyManager.json',
    returnCode: 6000,
    statusCode: 500,
  },
};

module.exports = errorCodes;
