const realmDTO = require('./realm-dto');
const keyDTO = require('./key-dto');
const emailDTO = require('./email-dto');
const tokenDTO = require('./token-dto');
const groupDTO = require('./group-dto');
const userDTO = require('./user-dto');
const roleDTO = require('./role-dto');
const permissionDTO = require('./permission-dto');

module.exports = {
  realmDTO,
  keyDTO,
  emailDTO,
  tokenDTO,
  groupDTO,
  userDTO,
  roleDTO,
  permissionDTO,
};
